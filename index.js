class Formatter {
  //add static methods here
  static capitalize(string){
    return string.charAt(0).toUpperCase()+string.splice(1)
  }
  static capitalize(string){
    return string.charAt(0).toUpperCase()+string.splice(1)
  }
}